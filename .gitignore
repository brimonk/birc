# ignore all object files
*.o

# also ignore makefile rules generation
*.d

# ignore .gdb_history
.gdb_history

# don't put databases in git history either
*.dat
*.db

# don't keep the binary in source control either
birc

# don't keep the data directory, it really should be separate
data/

